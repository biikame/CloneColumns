#pragma once 


#include <iostream>
#include <time.h>


enum defMax{
	maxX = 99,
	maxY = 99,
};


enum defaultData{

	defaultStageWidth = 8,
	defaultStageHeight = 17,

	defaultWidth = 6,
	defaultHeight = 13,

	defaultDropWidth = 1,
	defaultDropHeight = 3,

	defaultDammyHeight = 3,
};


enum StateData{
	INIT,
};


enum randCData{
	minRand = 0,
	maxRand = 6,
};

enum BLOCKDATA{
	SPACE,
	BLOCK,

};